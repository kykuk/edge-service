apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: polar-ingress
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              serviceName: edge-service
              servicePort: 80
